const [DEFAULT_ROUTING, ROUTING_RULE, ROUTING_RULE_ELSE, SKIP_CONDITION] = [
  0,
  1,
  2,
];

const LINK_MESSAGES = [
  "Default routing",
  "Routing rule",
  "Routing else",
  "Skip condition",
];

export {
  DEFAULT_ROUTING,
  ROUTING_RULE,
  ROUTING_RULE_ELSE,
  SKIP_CONDITION,
  LINK_MESSAGES,
};
